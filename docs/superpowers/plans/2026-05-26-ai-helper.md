# AI Helper Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add an "AI helper" button to the header that sends a test message to DeepSeek and shows the response in a sonner toast.

**Architecture:** New FSD feature slice `ai-helper` with an RTK Query mutation endpoint; a Next.js POST route handler calls the DeepSeek API server-side. `AiHelperButton` (client component) is composed into the existing `Header` server component.

**Tech Stack:** Next.js App Router route handlers, RTK Query (`injectEndpoints`), `sonner` toast, lucide-react `Bot` icon, CSS Modules, Vitest + React Testing Library.

---

## File Map

| Action | Path | Responsibility |
|--------|------|----------------|
| Create | `src/app/api/ai-helper/route.ts` | POST handler — calls DeepSeek API with server-side API key |
| Create | `src/features/ai-helper/api/aiHelperApi.ts` | RTK Query mutation `sendTestMessage` |
| Create | `src/features/ai-helper/ui/AiHelperButton.tsx` | Client component — button + toast logic |
| Create | `src/features/ai-helper/ui/AiHelperButton.module.css` | Button styles |
| Create | `src/features/ai-helper/ui/AiHelperButton.test.tsx` | Component tests |
| Create | `src/features/ai-helper/index.ts` | Public export |
| Modify | `src/widgets/header/ui/Header.tsx` | Add `<AiHelperButton />` to nav |
| Modify | `.env.local` | Add `DEEPSEEK_API_KEY` |

---

## Task 1: Route handler

**Files:**
- Create: `src/app/api/ai-helper/route.ts`

- [ ] **Step 1: Create the route handler**

```typescript
// src/app/api/ai-helper/route.ts
import { NextRequest, NextResponse } from 'next/server';

export async function POST(_request: NextRequest) {
  const apiKey = process.env.DEEPSEEK_API_KEY;
  if (!apiKey) {
    return NextResponse.json(
      { error: 'DEEPSEEK_API_KEY not configured' },
      { status: 500 }
    );
  }

  try {
    const response = await fetch('https://api.deepseek.com/chat/completions', {
      method: 'POST',
      headers: {
        'Content-Type': 'application/json',
        Authorization: `Bearer ${apiKey}`,
      },
      body: JSON.stringify({
        model: 'deepseek-chat',
        messages: [{ role: 'user', content: 'Say hello in one sentence' }],
      }),
    });

    if (!response.ok) {
      return NextResponse.json(
        { error: 'DeepSeek API error' },
        { status: response.status }
      );
    }

    const data = await response.json();
    const message: string = data.choices[0].message.content;
    return NextResponse.json({ message });
  } catch {
    return NextResponse.json(
      { error: 'Failed to contact DeepSeek' },
      { status: 500 }
    );
  }
}
```

- [ ] **Step 2: Add DeepSeek API key to `.env.local`**

Open `.env.local` and add:
```
DEEPSEEK_API_KEY=<your-key-from-platform.deepseek.com>
```

- [ ] **Step 3: Commit**

```bash
git add src/app/api/ai-helper/route.ts
git commit -m "feat(ai-helper): add DeepSeek route handler"
```

---

## Task 2: RTK Query mutation

**Files:**
- Create: `src/features/ai-helper/api/aiHelperApi.ts`

- [ ] **Step 1: Create the API slice**

```typescript
// src/features/ai-helper/api/aiHelperApi.ts
import { baseApi } from '@/shared/api/baseApi';

export const aiHelperApi = baseApi.injectEndpoints({
  endpoints: (builder) => ({
    sendTestMessage: builder.mutation<{ message: string }, void>({
      query: () => ({
        url: 'ai-helper',
        method: 'POST',
      }),
    }),
  }),
});

export const { useSendTestMessageMutation } = aiHelperApi;
```

- [ ] **Step 2: Commit**

```bash
git add src/features/ai-helper/api/aiHelperApi.ts
git commit -m "feat(ai-helper): add RTK Query sendTestMessage mutation"
```

---

## Task 3: AiHelperButton component (TDD)

**Files:**
- Create: `src/features/ai-helper/ui/AiHelperButton.test.tsx`
- Create: `src/features/ai-helper/ui/AiHelperButton.tsx`
- Create: `src/features/ai-helper/ui/AiHelperButton.module.css`

- [ ] **Step 1: Write the failing tests**

```tsx
// src/features/ai-helper/ui/AiHelperButton.test.tsx
import { render, screen, fireEvent, waitFor } from '@testing-library/react';
import { describe, it, expect, vi, beforeEach } from 'vitest';
import { AiHelperButton } from './AiHelperButton';
import { useSendTestMessageMutation } from '../api/aiHelperApi';

vi.mock('sonner', () => ({
  toast: { error: vi.fn(), success: vi.fn() },
}));

const mockSendTestMessage = vi.fn();

vi.mock('../api/aiHelperApi', () => ({
  useSendTestMessageMutation: vi.fn(() => [mockSendTestMessage, { isLoading: false }]),
}));

describe('AiHelperButton', () => {
  beforeEach(() => {
    vi.clearAllMocks();
    vi.mocked(useSendTestMessageMutation).mockReturnValue([mockSendTestMessage, { isLoading: false }] as any);
  });

  it('renders a button with aria-label "AI helper"', () => {
    render(<AiHelperButton />);
    expect(screen.getByRole('button', { name: /ai helper/i })).toBeInTheDocument();
  });

  it('calls sendTestMessage and shows success toast on click', async () => {
    const { toast } = await import('sonner');
    mockSendTestMessage.mockReturnValue({
      unwrap: () => Promise.resolve({ message: 'Hello from DeepSeek!' }),
    });

    render(<AiHelperButton />);
    fireEvent.click(screen.getByRole('button', { name: /ai helper/i }));

    await waitFor(() => {
      expect(mockSendTestMessage).toHaveBeenCalledTimes(1);
      expect(toast.success).toHaveBeenCalledWith('Hello from DeepSeek!');
    });
  });

  it('shows error toast when mutation rejects', async () => {
    const { toast } = await import('sonner');
    mockSendTestMessage.mockReturnValue({
      unwrap: () => Promise.reject(new Error('network error')),
    });

    render(<AiHelperButton />);
    fireEvent.click(screen.getByRole('button', { name: /ai helper/i }));

    await waitFor(() => {
      expect(toast.error).toHaveBeenCalledWith('AI helper failed to respond');
    });
  });

  it('disables the button while loading', () => {
    vi.mocked(useSendTestMessageMutation).mockReturnValue([mockSendTestMessage, { isLoading: true }] as any);

    render(<AiHelperButton />);
    expect(screen.getByRole('button', { name: /ai helper/i })).toBeDisabled();
  });
});
```

- [ ] **Step 2: Run tests — verify they fail**

```bash
npm run test:run -- src/features/ai-helper/ui/AiHelperButton.test.tsx
```

Expected: FAIL — `AiHelperButton` not found.

- [ ] **Step 3: Create the component**

```tsx
// src/features/ai-helper/ui/AiHelperButton.tsx
'use client';

import { Bot } from 'lucide-react';
import { toast } from 'sonner';
import { useSendTestMessageMutation } from '../api/aiHelperApi';
import styles from './AiHelperButton.module.css';

export const AiHelperButton: React.FC = () => {
  const [sendTestMessage, { isLoading }] = useSendTestMessageMutation();

  const handleClick = async () => {
    try {
      const result = await sendTestMessage().unwrap();
      toast.success(result.message);
    } catch {
      toast.error('AI helper failed to respond');
    }
  };

  return (
    <button
      className={styles.button}
      onClick={handleClick}
      disabled={isLoading}
      aria-label="AI helper"
    >
      <Bot size={20} className={isLoading ? styles.loading : undefined} />
    </button>
  );
};
```

- [ ] **Step 4: Create CSS Module**

```css
/* src/features/ai-helper/ui/AiHelperButton.module.css */
.button {
  display: flex;
  align-items: center;
  justify-content: center;
  background: none;
  border: none;
  cursor: pointer;
  color: inherit;
  padding: 0.25rem;
  border-radius: 0.25rem;
  transition: opacity 0.2s;
}

.button:disabled {
  cursor: not-allowed;
}

.loading {
  opacity: 0.4;
}
```

- [ ] **Step 5: Run tests — verify they pass**

```bash
npm run test:run -- src/features/ai-helper/ui/AiHelperButton.test.tsx
```

Expected: 4 tests PASS.

- [ ] **Step 6: Commit**

```bash
git add src/features/ai-helper/ui/
git commit -m "feat(ai-helper): add AiHelperButton component with tests"
```

---

## Task 4: Public export + header integration

**Files:**
- Create: `src/features/ai-helper/index.ts`
- Modify: `src/widgets/header/ui/Header.tsx`

- [ ] **Step 1: Create feature index**

```typescript
// src/features/ai-helper/index.ts
export { AiHelperButton } from './ui/AiHelperButton';
```

- [ ] **Step 2: Add button to Header**

Current `src/widgets/header/ui/Header.tsx`:
```tsx
import { createClient } from '@/shared/api/supabase/server';
import Link from 'next/link';
import { getTranslations } from 'next-intl/server';
import { UserMenu } from './UserMenu';
import styles from './Header.module.css';

export const Header = async () => {
  const supabase = await createClient();
  const { data: { user } } = await supabase.auth.getUser();
  const t = await getTranslations('Common');
  const authT = await getTranslations('Auth');

  return (
    <header className={styles.header}>
      <Link href="/" className={styles.logo}>
        {t('title')}
      </Link>
      <nav className={styles.nav}>
        {user ? (
          <UserMenu email={user.email} />
        ) : (
          <div className={styles.authLinks}>
            <UserMenu />
            <Link href="/login" className={styles.loginLink}>
              {authT('login')}
            </Link>
          </div>
        )}
      </nav>
    </header>
  );
};
```

Replace with:
```tsx
import { createClient } from '@/shared/api/supabase/server';
import Link from 'next/link';
import { getTranslations } from 'next-intl/server';
import { UserMenu } from './UserMenu';
import { AiHelperButton } from '@/features/ai-helper';
import styles from './Header.module.css';

export const Header = async () => {
  const supabase = await createClient();
  const { data: { user } } = await supabase.auth.getUser();
  const t = await getTranslations('Common');
  const authT = await getTranslations('Auth');

  return (
    <header className={styles.header}>
      <Link href="/" className={styles.logo}>
        {t('title')}
      </Link>
      <nav className={styles.nav}>
        <AiHelperButton />
        {user ? (
          <UserMenu email={user.email} />
        ) : (
          <div className={styles.authLinks}>
            <UserMenu />
            <Link href="/login" className={styles.loginLink}>
              {authT('login')}
            </Link>
          </div>
        )}
      </nav>
    </header>
  );
};
```

- [ ] **Step 3: Run full test suite**

```bash
npm run test:run
```

Expected: all tests PASS (including previously passing ones).

- [ ] **Step 4: Commit**

```bash
git add src/features/ai-helper/index.ts src/widgets/header/ui/Header.tsx
git commit -m "feat(ai-helper): integrate AiHelperButton into header"
```

---

## Task 5: Manual verification

- [ ] **Step 1: Start dev server**

```bash
npm run dev
```

- [ ] **Step 2: Verify in browser**

1. Open `http://localhost:3000`
2. Confirm `Bot` icon appears in the header to the left of the user menu icon
3. Click the button
4. Confirm a success toast appears with DeepSeek's response
5. Check network tab: `POST /api/ai-helper` returns `{ message: "..." }`

- [ ] **Step 3: Run linter**

```bash
npm run lint
```

Expected: no errors.
