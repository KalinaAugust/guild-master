# Project-scoped Rules

## Testing Workflow
- **Do not** run tests (`pnpm test` / `vitest`) after every small CSS or text modification.
- Only run tests after major structural component logic changes or when verifying the work before committing / finalizing the branch.
