import { DetailLayoutSkeleton } from '@/shared/ui/DetailLayout';

/** Streaming fallback for the event detail page. */
export default function EventDetailLoading() {
  return <DetailLayoutSkeleton />;
}
